version: '3.8'

services:
  pulse-node:
    build: .
    container_name: deep-pulse-node
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    network_mode: host # Allows localhost bridge to deep-ledger
    restart: unless-stopped
